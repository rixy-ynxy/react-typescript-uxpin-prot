"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const getConfigPath_1 = require("../getConfigPath");
describe('getConfigPath', () => {
    describe('returning absolute path of the config file', () => {
        it('resolves path relatively to the project root when the given path is relative', () => {
            // given
            const args = {
                config: '../directory/file.js',
                cwd: '/project/root/dir',
            };
            // then
            expect(getConfigPath_1.getConfigPath(args)).toEqual('/project/root/directory/file.js');
        });
    });
    it('returns absolute path, when the given path is absolute', () => {
        // given
        const args = {
            config: '/absolute/directory/file.js',
            cwd: '/project/root/dir',
        };
        // then
        expect(getConfigPath_1.getConfigPath(args)).toEqual('/absolute/directory/file.js');
    });
});
//# sourceMappingURL=getConfigPath.test.js.map
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const Environment_1 = require("../Environment");
const isTestEnv_1 = require("../isTestEnv");
describe('isTestEnv', () => {
    describe('test env', () => {
        let initialEnv;
        beforeEach(() => {
            initialEnv = process.env.UXPIN_ENV;
            // when
            process.env.UXPIN_ENV = Environment_1.Environment.TEST;
        });
        afterEach(() => {
            process.env.UXPIN_ENV = initialEnv;
        });
        it('should return true for test environment', () => {
            // then
            expect(isTestEnv_1.isTestEnv()).toBe(true);
        });
    });
    describe('different env', () => {
        let initialEnv;
        beforeEach(() => {
            initialEnv = process.env.UXPIN_ENV;
            // when
            process.env.UXPIN_ENV = Environment_1.Environment.PRODUCTION;
        });
        afterEach(() => {
            process.env.UXPIN_ENV = initialEnv;
        });
        it('should return false for different environment', () => {
            // then
            expect(isTestEnv_1.isTestEnv()).toBe(false);
        });
    });
});
//# sourceMappingURL=isTestEnv.test.js.map
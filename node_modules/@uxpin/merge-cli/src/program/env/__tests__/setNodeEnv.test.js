"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const Environment_1 = require("../Environment");
const setNodeEnv_1 = require("../setNodeEnv");
describe('setNodeEnv', () => {
    let initialEnv;
    beforeEach(() => {
        initialEnv = process.env.UXPIN_ENV;
    });
    afterEach(() => {
        process.env.UXPIN_ENV = initialEnv;
    });
    it('should set correct environment', () => {
        // when
        setNodeEnv_1.setNodeEnv(Environment_1.Environment.TEST);
        // then
        expect(process.env.UXPIN_ENV).toEqual(Environment_1.Environment.TEST);
    });
});
//# sourceMappingURL=setNodeEnv.test.js.map
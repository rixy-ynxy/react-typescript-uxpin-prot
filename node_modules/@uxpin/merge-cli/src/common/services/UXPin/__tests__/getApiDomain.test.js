"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const getApiDomain_1 = require("../getApiDomain");
describe('getApiDomain', () => {
    it('should return valid api domain url', () => {
        // given
        const domain = 'uxpin.com';
        // when
        const apiDomain = getApiDomain_1.getApiDomain(domain);
        // then
        expect(apiDomain).toEqual('https://uxpin.com');
    });
    it('should be able to specify protocol', () => {
        // given
        const domain = 'uxpin.com';
        const protocol = 'http';
        // when
        const apiDomain = getApiDomain_1.getApiDomain(domain, protocol);
        // then
        expect(apiDomain).toEqual('http://uxpin.com');
    });
});
//# sourceMappingURL=getApiDomain.test.js.map
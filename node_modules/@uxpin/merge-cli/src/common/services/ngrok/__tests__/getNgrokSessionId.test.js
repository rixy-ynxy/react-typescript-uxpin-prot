"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const getNgrokSessionId_1 = require("../getNgrokSessionId");
describe('getNgrokSessionId', () => {
    it('should return session id when correct url is provided', () => {
        // given
        const url = 'https://d8bbcb13.ngrok.io';
        // when
        const sessionId = getNgrokSessionId_1.getNgrokSessionId(url);
        // then
        expect(sessionId).toEqual('d8bbcb13');
    });
    it('should return undefined if it cant extract session id from url', () => {
        // given
        const url = '';
        // when
        const sessionId = getNgrokSessionId_1.getNgrokSessionId(url);
        // then
        expect(sessionId).toBeUndefined();
    });
    it('should return undefined when URL is not provided', () => {
        // given
        // when
        const sessionId = getNgrokSessionId_1.getNgrokSessionId();
        // then
        expect(sessionId).toBeUndefined();
    });
});
//# sourceMappingURL=getNgrokSessionId.test.js.map
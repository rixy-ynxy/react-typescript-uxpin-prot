"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.stringifyWarning = void 0;
const cleanStacktrace = require("clean-stacktrace");
const cleanStacktraceRelativePaths = require("clean-stacktrace-relative-paths");
const safe = require("colors/safe");
function stringifyWarning(warning, includeError = false) {
    const { message, sourcePath, originalError } = warning;
    let result = safe.yellow('warning ') + message + ' in:\n';
    if (sourcePath) {
        result += safe.blue(sourcePath) + '\n';
    }
    if (includeError && originalError && originalError.stack) {
        result += cleanStacktrace(originalError.stack, cleanStacktraceRelativePaths()) + '\n';
    }
    return result;
}
exports.stringifyWarning = stringifyWarning;
//# sourceMappingURL=stringifyWarning.js.map
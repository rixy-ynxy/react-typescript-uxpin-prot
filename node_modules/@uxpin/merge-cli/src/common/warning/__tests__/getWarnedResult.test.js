"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const getWarnedResult_1 = require("../getWarnedResult");
describe('getWarnedResult', () => {
    describe('when result is given', () => {
        it('should be wrapped by object with warnings', () => {
            // having
            const result = { name: 'Donald' };
            // when
            const warned = getWarnedResult_1.getWarnedResult(result);
            // then
            expect(warned).toEqual({
                result: { name: 'Donald' },
                warnings: [],
            });
        });
    });
    describe('when result and warnings are given', () => {
        it('should be wrapped by object with warinings', () => {
            // having
            const result = { name: 'Donald' };
            const warnings = [
                { message: 'foo' },
            ];
            // when
            const warned = getWarnedResult_1.getWarnedResult(result, warnings);
            // then
            expect(warned).toEqual({
                result: { name: 'Donald' },
                warnings: [
                    { message: 'foo' },
                ],
            });
        });
    });
});
//# sourceMappingURL=getWarnedResult.test.js.map
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.prepareDataFromPayload = void 0;
const querystring_1 = require("querystring");
function prepareDataFromPayload(request) {
    return new Promise((resolve, reject) => {
        let body = '';
        request.on('data', (chunk) => {
            body += chunk.toString();
        });
        request.on('end', () => {
            if (body.startsWith('json')) {
                const data = querystring_1.parse(body);
                resolve(JSON.parse(data.json));
            }
            else {
                resolve(JSON.parse(body));
            }
        });
        request.on('error', (error) => {
            reject(error);
        });
    });
}
exports.prepareDataFromPayload = prepareDataFromPayload;
//# sourceMappingURL=prepareDataFromPayload.js.map
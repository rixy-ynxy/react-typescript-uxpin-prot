"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const path = require("path");
const getConfig_1 = require("../../../building/config/getConfig");
const getEPIDFilePath_1 = require("../getEPIDFilePath");
describe('getEPIDFilePath', () => {
    it('should returns epid file path for current directory', () => {
        // given
        const projectRoot = __dirname;
        // when
        // then
        const expectedFilePath = path.join(projectRoot, getConfig_1.TEMP_DIR_PATH, getEPIDFilePath_1.EPID_FILENAME);
        expect(getEPIDFilePath_1.getEPIDFilePath(projectRoot)).toEqual(expectedFilePath);
    });
    it('should returns epid file path for different directory than current', () => {
        // given
        const projectRoot = path.resolve(__dirname, '..');
        // when
        // then
        const expectedFilePath = path.join(projectRoot, getConfig_1.TEMP_DIR_PATH, getEPIDFilePath_1.EPID_FILENAME);
        expect(getEPIDFilePath_1.getEPIDFilePath(projectRoot)).toEqual(expectedFilePath);
    });
});
//# sourceMappingURL=getEPIDFilePath.test.js.map
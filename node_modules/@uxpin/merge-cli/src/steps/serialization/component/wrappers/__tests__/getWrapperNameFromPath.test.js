"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const getWrapperNameFromPath_1 = require("../getWrapperNameFromPath");
describe('getWrapperNameFromPath', () => {
    describe('when path is provided', () => {
        it('should return file name as a wrapper name', () => {
            // having
            const path = './path/to/CustomWrapper.ts';
            // when
            // then
            expect(getWrapperNameFromPath_1.getWrapperNameFromPath(path)).toEqual('CustomWrapper');
        });
    });
    describe('when path is provided which contains built in component name', () => {
        it('should return file name with prefix as a wrapper name', () => {
            // having
            const path = './path/to/NonResizableWrapper.ts';
            // when
            // then
            expect(getWrapperNameFromPath_1.getWrapperNameFromPath(path)).toEqual('CustomNonResizableWrapper');
        });
    });
});
//# sourceMappingURL=getWrapperNameFromPath.test.js.map
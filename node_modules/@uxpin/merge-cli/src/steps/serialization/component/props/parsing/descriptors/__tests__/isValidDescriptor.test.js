"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const using_1 = require("../../../../../../../../test/utils/using");
const ComponentPropertyDefinition_1 = require("../../../../implementation/ComponentPropertyDefinition");
const isValidDescriptor_1 = require("../isValidDescriptor");
const cases = [
    ...Object.values(ComponentPropertyDefinition_1.CustomDescriptorsTags).map((descriptor) => ({
        descriptor,
        expectedValue: true,
    })),
    {
        descriptor: '@somefaketag',
        expectedValue: false,
    },
];
describe('isValidDescriptor', () => {
    using_1.using(cases)
        .describe('correctly returns paths for', (testCase) => {
        const { descriptor, expectedValue } = testCase;
        it(`should return ${expectedValue} for ${descriptor}`, () => {
            expect(isValidDescriptor_1.isValidDescriptor(descriptor)).toEqual(expectedValue);
        });
    });
});
//# sourceMappingURL=isValidDescriptor.test.js.map
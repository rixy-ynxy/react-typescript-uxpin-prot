"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const getComponentNamespacedName_1 = require("../getComponentNamespacedName");
describe('getComponentNamespacedName', () => {
    it('should get valid name for component without namespace', () => {
        // having
        const metadata = {
            defaultExported: true,
            name: 'Button',
            properties: [],
        };
        // when
        // then
        expect(getComponentNamespacedName_1.getComponentNamespacedName(metadata)).toBe('Button');
    });
    it('should get valid name for component with namespace', () => {
        // having
        const metadata = {
            defaultExported: true,
            name: 'Button',
            namespace: {
                importSlug: 'Card_Header_Button',
                name: 'Card.Header',
            },
            properties: [],
        };
        // when
        // then
        expect(getComponentNamespacedName_1.getComponentNamespacedName(metadata)).toBe('Card.Header.Button');
    });
});
//# sourceMappingURL=getComponentNamespacedName.test.js.map
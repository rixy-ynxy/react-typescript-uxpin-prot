"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const getLines_1 = require("../getLines");
describe('getLines', () => {
    describe('when string is given', () => {
        it('should return trimmed lines array', () => {
            // having
            // tslint:disable:no-trailing-whitespace
            const value = `

multiline
text with      
    whitespaces and
    stuff
    
    
    
    `;
            // tslint:enable:no-trailing-whitespace
            // when
            const lines = getLines_1.getLines(value);
            // then
            expect(lines).toEqual([
                'multiline',
                'text with',
                'whitespaces and',
                'stuff',
            ]);
        });
    });
});
//# sourceMappingURL=getLines.test.js.map
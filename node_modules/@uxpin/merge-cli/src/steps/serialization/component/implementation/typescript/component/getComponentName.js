"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getComponentName = void 0;
const typescript_1 = require("typescript");
const getComponentNameFromPath_1 = require("../../../name/getComponentNameFromPath");
function getComponentName(context, component) {
    if (component.name) {
        return component.name.getText();
    }
    // Support named arrow functions and use the component parent's name.
    // https://github.com/UXPin/uxpin-merge-tools/issues/208
    if (typescript_1.isArrowFunction(component) && component.parent &&
        'name' in component.parent && component.parent.name) {
        return component.parent.name.getText();
    }
    return getComponentNameFromPath_1.getComponentNameFromPath(context.componentPath);
}
exports.getComponentName = getComponentName;
//# sourceMappingURL=getComponentName.js.map
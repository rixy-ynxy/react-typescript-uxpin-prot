"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getPropertyDescription = void 0;
const getLines_1 = require("../../../comments/getLines");
const GENERAL_JSDOC_TAG_PREFIX = '@';
const UXPIN_JSDOC_TAG_PREFIX = '@uxpin';
function getPropertyDescription(desc = '') {
    const lines = getLines_1.getLines(desc);
    const descLines = [];
    let isUXPinTagOpen = false;
    for (let i = 0; i < lines.length; i += 1) {
        const prevLine = lines[i - 1] || '';
        const line = lines[i];
        isUXPinTagOpen = line.startsWith(UXPIN_JSDOC_TAG_PREFIX)
            || isUXPinTagOpen && !line.startsWith(GENERAL_JSDOC_TAG_PREFIX);
        if ((line === '' && prevLine === '')
            || isUXPinTagOpen) {
            continue;
        }
        descLines.push(line);
    }
    return descLines.join(getLines_1.LINES_DELIMETER).trim();
}
exports.getPropertyDescription = getPropertyDescription;
//# sourceMappingURL=getPropertyDescription.js.map
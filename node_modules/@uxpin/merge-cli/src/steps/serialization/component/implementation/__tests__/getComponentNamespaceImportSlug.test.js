"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const getComponentNamespaceImportSlug_1 = require("../getComponentNamespaceImportSlug");
describe('getComponentNamespaceImportSlug', () => {
    const componentName = 'ComponentName';
    describe('given single level namespace name', () => {
        const namespaceName = 'SingleNamespace';
        let importSlug;
        beforeAll(() => {
            importSlug = getComponentNamespaceImportSlug_1.getComponentNamespaceImportSlug(namespaceName, componentName);
        });
        it('joins namespace and component name with delimiter', () => {
            expect(importSlug).toEqual('SingleNamespace_ComponentName');
        });
    });
    describe('given multi-level namespace name', () => {
        const namespaceName = 'Level1.level2.Namespace';
        let importSlug;
        beforeAll(() => {
            importSlug = getComponentNamespaceImportSlug_1.getComponentNamespaceImportSlug(namespaceName, componentName);
        });
        it('creates import string replacing original namespace delimiter', () => {
            expect(importSlug).toEqual('Level1_level2_Namespace_ComponentName');
        });
    });
});
//# sourceMappingURL=getComponentNamespaceImportSlug.test.js.map
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const path_1 = require("path");
const execAsync_1 = require("../../../../../../../utils/child_process/execAsync");
const getMovedFiles_1 = require("../getMovedFiles");
describe('getMovedFiles', () => {
    it('should get map of moved files', () => tslib_1.__awaiter(void 0, void 0, void 0, function* () {
        // given
        const cwd = path_1.resolve(__dirname, '../../../../../../../../test/resources/repos/git-repo-rename');
        const hashes = yield execAsync_1.execAsync('git log -n 2 --pretty="format:%H"', { cwd });
        const [revision2, revision1] = hashes.split('\n');
        // when
        const files = yield getMovedFiles_1.getMovedFiles(cwd, revision1, revision2);
        // then
        expect(files).toEqual({
            'Component.js': 'RenamedComponent.js',
        });
    }));
});
//# sourceMappingURL=getMovedFiles.test.js.map
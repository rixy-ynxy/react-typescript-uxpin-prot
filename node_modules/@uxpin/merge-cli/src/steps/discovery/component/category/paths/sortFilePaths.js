"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.sortFilePaths = void 0;
const path_1 = require("path");
function sortFilePaths(paths) {
    return paths.sort(sorter);
}
exports.sortFilePaths = sortFilePaths;
function sorter(fullPathA, fullPathB) {
    const parsedPathA = path_1.parse(fullPathA);
    const parsedPathB = path_1.parse(fullPathB);
    if (parsedPathA.dir === parsedPathB.dir) {
        return sortAZ(`${parsedPathA.name}${parsedPathA.ext}`, `${parsedPathB.name}${parsedPathB.ext}`);
    }
    if (parsedPathA.dir.includes(parsedPathB.dir)) {
        return 1;
    }
    if (parsedPathB.dir.includes(parsedPathA.dir)) {
        return -1;
    }
    return sortAZ(parsedPathA.dir, parsedPathB.dir);
}
function sortAZ(a, b) {
    return a > b
        ? 1
        : a < b
            ? -1
            : 0;
}
//# sourceMappingURL=sortFilePaths.js.map
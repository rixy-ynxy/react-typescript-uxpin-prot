"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getSortIndex = exports.isPreset = exports.getPresetName = void 0;
const path_1 = require("path");
const PRESET_FILENAME_REGEX = /^(\d+)-(.+)\.jsx$/i;
const PRESET_NAME_INDEX_IN_MATCH_RESULT = 2;
function getPresetName(path) {
    const fileName = path_1.basename(path);
    const found = fileName.match(PRESET_FILENAME_REGEX);
    if (found && found[PRESET_NAME_INDEX_IN_MATCH_RESULT]) {
        return found[PRESET_NAME_INDEX_IN_MATCH_RESULT];
    }
    return '';
}
exports.getPresetName = getPresetName;
function isPreset(path) {
    const fileName = path_1.basename(path);
    return PRESET_FILENAME_REGEX.test(fileName);
}
exports.isPreset = isPreset;
function getSortIndex(path) {
    const fileName = path_1.basename(path);
    const found = fileName.match(PRESET_FILENAME_REGEX);
    if (found && found[1]) {
        return parseInt(found[1], 10);
    }
    return Infinity;
}
exports.getSortIndex = getSortIndex;
//# sourceMappingURL=presetFileNameParser.js.map
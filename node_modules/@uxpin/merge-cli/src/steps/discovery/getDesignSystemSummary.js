"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getDesignSystemSummary = void 0;
const safe = require("colors/safe");
function getDesignSystemSummary(categories) {
    return categories.map(describeCategory).join('\n');
}
exports.getDesignSystemSummary = getDesignSystemSummary;
function describeCategory(category) {
    return `${safe.bold(category.name)}
${category.components.map(describeComponent).join('')}`;
}
function describeComponent(component) {
    return `
    ${safe.bold(component.name)}
        📜 documentation: ${booleanToCheckmark(!!component.info.documentation)}
        💡 examples: ${booleanToCheckmark(component.documentation.examples.length > 0)}
        🎛  presets: ${booleanToCheckmark(component.presets.length > 0)}
`;
}
function booleanToCheckmark(value) {
    if (value) {
        return safe.green('✔');
    }
    return safe.red('✘');
}
//# sourceMappingURL=getDesignSystemSummary.js.map